# Country Pack Contract

Country Pack is server-side IP.

A pack may contain:

- field coordinates
- field types
- business mapping
- dropdown rules
- SafeBrake policy
- error map
- geometry requirements

The local Agent must never receive the complete pack.

The Control Plane compiles only the minimum steps required for a given job and signs them.
