{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CustomsOps Declaration Model",
  "type": "object",
  "required": ["declaration_id", "template_type", "items", "source"],
  "properties": {
    "declaration_id": { "type": "string" },
    "template_type": {
      "type": "string",
      "enum": ["Sheet1", "Combine", "Simplified", "Separate"]
    },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["source_row", "awb", "document_reference", "hs_code", "quantity", "statistical_quantity", "gross_weight", "net_weight", "invoice_value", "origin", "description"],
        "properties": {
          "source_row": { "type": "integer", "minimum": 1 },
          "awb": { "type": "string" },
          "document_reference": { "type": "string" },
          "hs_code": { "type": "string" },
          "quantity": { "type": "number" },
          "statistical_quantity": { "type": "number" },
          "gross_weight": { "type": "number" },
          "net_weight": { "type": "number" },
          "invoice_value": { "type": "number" },
          "origin": { "type": "string" },
          "description": { "type": "string" }
        }
      }
    },
    "source": {
      "type": "object",
      "required": ["manifest_sha256"],
      "properties": {
        "manifest_sha256": { "type": "string" },
        "filename": { "type": "string" }
      }
    }
  }
}
