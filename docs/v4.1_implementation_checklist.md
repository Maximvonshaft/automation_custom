# v4.1 Implementation Checklist

## Manifest and Declaration Model

- [ ] Classify fixed Excel template sheets: `Sheet1`, `Combine`, `Simplified`, `Separate`.
- [ ] Parse sanitized `Combine` and `Separate` rows.
- [ ] Produce structured parse reports.
- [ ] Build neutral declaration models with source row lineage.
- [ ] Keep executable GUI-step compilation out of the Agent.

## Signed Job Compiler

- [ ] Compile declaration model input server-side.
- [ ] Preserve `fillOnly` and `safeBrakeStore` only.
- [ ] Inject SafeBrake policy server-side.
- [ ] Reject forbidden finalization actions.

## Windows GUI Executor

- [ ] Add executor interface.
- [ ] Keep mock executor for CI and dry runs.
- [ ] Add Windows-only GUI executor adapter.
- [ ] Fail closed on non-Windows real execution.
- [ ] Require ASYCUDA foreground window confirmation.

## v3.1.1 Evidence Regression

- [ ] Produce `ledger.jsonl`.
- [ ] Produce `run_context.json`.
- [ ] Include manifest parse report and hashes.
- [ ] Preserve screenshot folder and review bundle semantics.

