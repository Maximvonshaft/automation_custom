# v4.1 Windows ASYCUDA Lab Smoke Result Template

## Required Fields

- `run_id`:
- `machine_id`:
- `tenant_id`:
- `ASYCUDA window title`:
- `pack_version`:
- `job_id`:
- `mode`:
- `screenshot count`:
- `ledger step count`:
- `SafeBrake result`:
- `known issues`:

## Evidence References

- `review_bundle.zip` path or artifact URL:
- `manifest_parse_report.json` hash:
- `declaration_model.json` hash:
- `signed_job_plan.json` hash:
- `ledger.jsonl` hash:
- `screenshots/` hash summary:

## Boundary Confirmation

- Submit/Register/Payment/tax-finalizing automation was not run:
- Country pack content was not present in the Agent artifact:
- Local executable job compilation from Excel was not performed:

