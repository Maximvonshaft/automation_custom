# v4.2 Implementation Roadmap

## Purpose

This roadmap decomposes v4.2 Controlled Operator Package into implementation milestones. Each milestone was delivered through focused PRs with isolated acceptance criteria.

## Current baseline

- v4.1 is an internal Release Candidate.
- Windows ASYCUDA Lab Smoke passed under SafeBrake.
- Excel manifest intake supports current Albania baseline and non-merge frontline template regression.
- Signed job plan validation exists.
- Windows GUI executor path exists.
- Evidence bundle path exists.
- v4.2 implementation milestones are complete through controlled pilot gate.
- External operator package rollout is not approved by these implementation milestones.

## Milestone 0: Planning pack

Status: merged.

Deliverables:

- v4.2 controlled package plan;
- security boundary;
- acceptance criteria;
- release packaging checklist;
- file-level implementation roadmap.

Non-scope:

- runtime code;
- executable installer;
- external release.

## Milestone 1: Packaging boundary audit

Status: merged.

Goal: prove what must and must not enter the local package.

Delivered work:

- identify package entrypoints;
- classify files as local-safe, server-only, test-only, or excluded;
- add package exclusion manifest;
- add tests that assert country pack production content is absent from local package;
- add tests that assert `control_plane/packs` does not ship with operator artifact.

Representative files:

- `docs/v4.2_package_file_classification.md`
- `packaging/operator_package_manifest.json`
- `tests/test_operator_package_boundaries.py`

## Milestone 2: Operator package skeleton

Status: merged.

Goal: define a local package structure without releasing it externally.

Delivered work:

- create Windows package layout;
- define launcher entrypoint;
- include operator runbook;
- include support diagnostics command;
- exclude country pack and server config;
- generate build manifest and checksum skeleton.

Representative files:

- `packaging/windows_operator/README.md`
- `packaging/windows_operator/build_manifest.schema.json`
- `packaging/windows_operator/package_skeleton.ps1`
- `scripts/operator_support_diagnostics.ps1`

## Milestone 3: Machine registration design implementation

Status: merged.

Goal: bind operator package to machine identity and tenant.

Delivered work:

- define machine identity contract;
- define local machine registration schema;
- define local machine config storage boundary;
- ensure machine_id is used during signed job verification;
- define active, pending, revoked, and disabled semantics.

Representative files:

- `shared/schemas/machine_registration.schema.json`
- `agent/customsops_agent/machine_identity.py`
- `docs/v4.2_machine_registration.md`
- `tests/test_machine_registration_contract.py`

Implementation must not include production secrets.

## Milestone 4: Signed-job-only operator flow

Status: merged.

Goal: ensure the operator package can only run HQ-issued signed jobs.

Delivered work:

- harden signed job import path;
- require active machine registration before execution;
- require trusted public key;
- delegate to existing signed-plan validation;
- reject expired / wrong tenant / wrong machine / unsupported mode / forbidden actions through existing validation path;
- keep local Excel-to-executable-plan compilation disabled;
- document signed-job-only behavior.

Representative files:

- `agent/customsops_agent/operator_flow.py`
- `docs/v4.2_signed_job_only_operator_flow.md`
- `packaging/windows_operator/operator_launcher.ps1`
- `tests/test_operator_package_skeleton.py`

## Milestone 5: Operator UX shell

Status: merged.

Goal: provide a simple operator workflow while preserving security boundaries.

Delivered work:

- status command for registration and public key readiness;
- superficial manifest precheck command for file existence, suffix, hash, ZIP integrity, and workbook metadata;
- signed job execution command delegating to `operator_flow.run_operator_signed_job(...)`;
- optional evidence folder open command;
- console entrypoint `customsops-operator`;
- explicit JSON boundary output proving no plan editor, no country pack viewer, and no local Excel compiler.

Representative files:

- `agent/customsops_agent/operator_cli.py`
- `docs/v4.2_operator_ux_shell.md`
- `tests/test_operator_cli_contract.py`
- `pyproject.toml`

## Milestone 6: Evidence reference model

Status: merged.

Goal: move from local bundle only to controlled HQ reference flow.

Delivered work:

- define `shared/schemas/evidence_reference.schema.json`;
- add reference-only evidence builder for local evidence ZIPs;
- add `operator_cli reference-evidence` command;
- represent evidence bundle, manifest, screenshot artifacts, and support diagnostics by hashes only;
- mark raw artifacts as not uploaded and screenshot content as not included;
- fail closed when evidence ZIP is invalid or missing `evidence_manifest.json`.

Representative files:

- `agent/customsops_agent/evidence_reference.py`
- `shared/schemas/evidence_reference.schema.json`
- `docs/v4.2_evidence_reference_model.md`
- `tests/test_evidence_reference_contract.py`

## Milestone 7: Controlled pilot package gate

Status: merged.

Goal: create a review gate for an internal pilot candidate without approving external distribution.

Delivered work:

- define `shared/schemas/pilot_gate_record.schema.json`;
- add operator and machine allowlist examples with default `approved=false`;
- add `pilot_gate_record.ps1` to validate candidate manifest/checksum inputs and write `PILOT_GATE_RECORD.json`;
- add controlled gate documentation;
- add operator package note template;
- add rollback and disable runbook;
- add contract tests proving the gate is record-only and not an external rollout approval.

Representative files:

- `shared/schemas/pilot_gate_record.schema.json`
- `packaging/windows_operator/pilot_gate_record.ps1`
- `packaging/windows_operator/operator_allowlist.example.json`
- `packaging/windows_operator/machine_allowlist.example.json`
- `docs/v4.2_controlled_pilot_gate.md`
- `docs/v4.2_operator_package_note_template.md`
- `docs/v4.2_pilot_rollback_disable.md`
- `tests/test_pilot_gate_contract.py`

Non-scope:

- installer generation;
- external rollout approval;
- country pack inclusion;
- local Excel-to-executable-plan compilation;
- raw sensitive evidence inclusion.

## Milestone sequencing

Delivered order:

1. Packaging boundary audit.
2. Operator package skeleton.
3. Machine registration contract.
4. Signed-job-only hardening.
5. Operator UX shell.
6. Evidence reference model.
7. Internal pilot package gate.

Do not begin external distribution before separate business, compliance, support, and technical approval.

## Validation command set

Every implementation PR ran or required:

```text
python 15_automation_scripts/validate_pack.py
python -m ruff check .
python -m pytest -q
python -m bandit -q -r agent control_plane shared
```

Package-specific PRs added relevant packaging tests and hash/gate evidence.

## Exit criteria for v4.2 implementation

v4.2 implementation may be considered complete when:

- package boundary audit passes;
- local artifact excludes server-only pack content;
- signed-job-only execution is tested;
- machine binding is tested;
- operator UX shell is tested;
- evidence reference path is tested;
- controlled pilot gate is tested;
- packaging checklist is complete;
- no-finalization boundary remains enforced;
- a separate approval record captures artifact hashes and target pilot machines before any actual pilot use.

Current status: implementation milestones are complete through M7; external rollout remains unapproved.
