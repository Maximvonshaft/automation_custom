# CODEX TASK 01 — Manifest Template Intake

Implement server-side Excel template intake for Albania manifest templates.

Requirements:

- Add template classifier for sheets: `Sheet1`, `Combine`, `Simplified`, `Separate`.
- P0 accepted: workbook containing `Combine` or `Separate` with expected headers.
- Add parse report with accepted/rejected sheets, missing headers, row counts, and warnings.
- Do not commit real rows from `235-97877625.xlsx`.
- Add sanitized fixture workbook/CSV with only synthetic rows.

Do not compile executable GUI steps in the Agent from Excel.
