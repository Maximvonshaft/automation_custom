# Albania ASYCUDA Country Pack

Internal-only placeholder pack for v4.0 controlled runtime tests.

Production coordinates, dropdown rules, resolver logic, and business mappings must stay server-side in
the Control Plane. The local Agent must never include this directory or a plaintext export of it.

