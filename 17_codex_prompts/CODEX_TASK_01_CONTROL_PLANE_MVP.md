# Codex Task 01 — Control Plane MVP

Implement a FastAPI Control Plane MVP:

- health endpoint;
- tenant/machine/license models;
- job compile endpoint;
- job fetch endpoint;
- evidence upload endpoint;
- admin revoke endpoint;
- audit event logging.

Use SQLite for local dev if necessary, but keep config compatible with PostgreSQL.

Do not put Country Pack files under Agent paths.
