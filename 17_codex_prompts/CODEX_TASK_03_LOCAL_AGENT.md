# Codex Task 03 — Local Agent

Implement the Local Operator Agent.

Requirements:

- load signed job plan;
- verify signature and policy;
- execute limited action grammar;
- support mock executor for CI;
- support real executor abstraction for Windows UI automation;
- generate ledger.jsonl;
- capture screenshots where available;
- build evidence bundle;
- include watermarks.

Do not include complete Country Pack in Agent.
