# Codex Task 04 — Country Pack Compiler

Implement server-side Country Pack loader and job compiler.

Requirements:

- server-side pack path only;
- compile minimal job steps;
- inject SafeBrake policy;
- generate `fillOnly` and `safeBrakeStore` jobs;
- never expose full pack to Agent;
- add test verifying Agent artifact has no pack files.
